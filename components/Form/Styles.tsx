import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
  input: {
    textAlign: "right",
    paddingRight: '10%',
    color: "white",
    minHeight: 40,
    fontSize: 50,
  },
});
